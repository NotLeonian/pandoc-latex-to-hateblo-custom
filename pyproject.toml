[project]
name = "pandoc-latex-to-hateblo-custom"
version = "2.0.1"
description = "LaTeXからはてな記法への変換用Pandocフィルタ"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "panflute>=2.3.1",
    "requests>=2.32.5",
    "types-requests>=2.32.4.20250913",
]

[dependency-groups]
dev = [
    "mypy>=1.20.0",
    "pyright>=1.1.408",
    "ruff>=0.15.9",
]

[tool.mypy]
exclude = '(^|/)_vendor/'
python_version = "3.13"

[tool.pyright]
exclude = [
    ".venv",
    "**/_vendor",
    "**/node_modules",
    "**/__pycache__",
    "**/.*",
]
pythonVersion = "3.13"

[tool.ruff]
extend-exclude = ["_vendor"]

[tool.ruff.format]
quote-style = "preserve"

[tool.ruff.lint]
extend-select = ["I"]

[[tool.mypy.overrides]]
module = "panflute"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "_vendor.*"
follow_imports = "skip"
